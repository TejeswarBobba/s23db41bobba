extends layout

block content
  h1= title
  - var watches = ['1','2','3','4','5']
     -for(var i=0; i<3; i++){
         - var randomno = Math.floor(Math.random()*5)+1;
           img(src=`/images/watch${randomno}.jpg`, "height"=300, "width"=300)
     -}